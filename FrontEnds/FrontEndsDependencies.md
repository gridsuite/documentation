# Frontends Production Dependencies

## UI / Design System

| Library | Description |
|---------|-------------|
| `@mui/material` | Material Design UI components (MUI v6) |
| `@mui/icons-material` | Material Design icons for MUI |
| `@mui/lab` | Experimental MUI components (not yet stabilized) |
| `@mui/x-charts` | MUI chart components |
| `@mui/x-tree-view` | MUI tree view component |
| `@mui/x-date-pickers` | MUI date and time picker components |
| `@emotion/react` | CSS-in-JS engine used by MUI |
| `@emotion/styled` | Styled-components API for Emotion |
| `mui-nested-menu` | Nested menus for MUI |
| `notistack` | Stacked snackbar notifications |
| `typeface-roboto` | Roboto font (used by Material Design) |
| `clsx` | Utility for conditionally composing CSS class names |
| `@material-symbols/svg-400` | Material Symbols SVG icons (weight 400) |
| `@react-querybuilder/material` | MUI theme adapter for react-querybuilder |
| `autosuggest-highlight` | Utility to highlight matched text in autosuggest inputs |

## React & Routing

| Library | Description |
|---------|-------------|
| `react` | Main UI library |
| `react-dom` | React rendering in the DOM |
| `react-router` | Client-side routing |

## State Management

| Library | Description |
|---------|-------------|
| `redux` | Global state store |
| `react-redux` | React/Redux bindings |
| `@reduxjs/toolkit` | Official Redux toolkit (simplifies Redux usage) |

## Forms & Validation

| Library | Description |
|---------|-------------|
| `react-hook-form` | Performant form management |
| `react-hook-form-mui` | MUI components integration for react-hook-form |
| `@hookform/resolvers` | Validation adapters for react-hook-form |
| `yup` | Declarative validation schemas |
| `zod` | TypeScript-first schema validation library |

## Tables & Grids

| Library | Description |
|---------|-------------|
| `ag-grid-community` | Advanced data grid (core) |
| `@ag-grid-community/locale` | AG Grid locale and translation utilities |
| `ag-grid-react` | React integration for AG Grid |
| `@tanstack/react-table` | Headless table management library |
| `react-grid-layout` | Resizable and drag-and-drop grid layout |

## Virtualization & Performance

| Library | Description |
|---------|-------------|
| `@tanstack/react-virtual` | List and table virtualization |
| `react-window` | Virtualized rendering for large lists |
| `react-virtualized-auto-sizer` | Automatically sizes virtualized list containers |

## Charts & Visualization

| Library | Description |
|---------|-------------|
| `plotly.js-basic-dist-min` | Interactive charting library (minimal distribution) |
| `react-plotly.js` | React wrapper for Plotly.js |
| `@svgdotjs/svg.js` | SVG manipulation and drawing |

## Diagrams & Flow

| Library | Description |
|---------|-------------|
| `@xyflow/react` | Interactive diagram/flow editor (React Flow) |
| `@powsybl/network-viewer` | Electrical network viewer (PowSyBl) |
| `react-querybuilder` | Query builder UI component |
| `@svgdotjs/svg.panzoom.js` | Pan & zoom plugin for SVG.js |

## Drag & Drop / Resizing

| Library | Description |
|---------|-------------|
| `@hello-pangea/dnd` | Accessible drag-and-drop (fork of react-beautiful-dnd) |
| `react-resizable` | Resizable components |
| `react-resizable-panels` | User-resizable panels |
| `react-rnd` | Resizable and draggable components |
| `react-dnd` | Low-level drag-and-drop for React |
| `react-dnd-html5-backend` | HTML5 backend for react-dnd |
| `@react-querybuilder/dnd` | Drag-and-drop plugin for react-querybuilder |

## Internationalization

| Library | Description |
|---------|-------------|
| `react-intl` | Internationalization (i18n) for React (FormatJS) |
| `localized-countries` | List of countries translated in multiple languages |

## Network & Communication

| Library | Description |
|---------|-------------|
| `reconnecting-websocket` | WebSocket with automatic reconnection |
| `qs` | Query string serialization/deserialization |

## Utilities

| Library | Description |
|---------|-------------|
| `mathjs` | Advanced mathematical computation library |
| `uuid` | UUID generation |
| `eventemitter3` | High-performance EventEmitter |
| `react-papaparse` | CSV file parsing (React wrapper for PapaParse) |
| `core-js` | JavaScript polyfills for older browsers |
| `date-fns` | Modern JavaScript date utility library |
| `lodash` | General-purpose JavaScript utility library |
| `prop-types` | Runtime type checking for React props |
| `ace-builds` | Embeddable code editor (Ace editor) |
| `jwt-decode` | Lightweight JWT token decoding |
| `type-fest` | Collection of essential TypeScript types |

## Authentication

| Library | Description |
|---------|-------------|
| `oidc-client-ts` | OpenID Connect and OAuth2 client library |

## Mapping & Geospatial

| Library | Description |
|---------|-------------|
| `mapbox-gl` | Interactive vector map rendering (Mapbox) |
| `maplibre-gl` | Open-source fork of Mapbox GL JS |
| `react-map-gl` | React wrapper for Mapbox/MapLibre GL JS |
| `@mapbox/mapbox-gl-draw` | Drawing tools plugin for Mapbox GL |
| `deck.gl` | WebGL2 framework for large-scale data visualization on maps |
| `@turf/boolean-point-in-polygon` | Geospatial point-in-polygon test (Turf.js) |
| `cheap-ruler` | Fast and accurate geodesic distance calculations |
| `geolib` | Geospatial calculations library (distance, bearing, etc.) |

